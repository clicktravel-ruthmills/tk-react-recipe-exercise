import React from 'react';
import 'styled-components/macro';
import { RecipeList } from '../../data/types';

type Props = {
    value: RecipeList,
};

const RecipeList = ({
    value,
}: Props) => {

};

export default RecipeList;
